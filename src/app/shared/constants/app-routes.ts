export const APP_ROUTE_PATHS = {
  onboarding: 'onboarding',
  allowLocation: 'allow-location',
  home: 'home',
  dashboard: 'dashboard'
} as const;

export const DEFAULT_APP_ROUTE = APP_ROUTE_PATHS.onboarding;
